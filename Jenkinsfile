@Library('slack') _

pipeline {
  agent {
      label 'jenkins-slave'
	}

  stages {
      stage('Build Artifact') {
            steps {
              sh 'printenv'
            }
        } 
      } 





